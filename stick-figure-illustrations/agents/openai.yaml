interface:
  display_name: "Stick Figure Illustrations"
  short_description: "Turn creator content into emotionally expressive stick-figure sketch illustrations"
  default_prompt: "Use $stick-figure-illustrations to turn this idea or content into a stick-figure sketch illustration, using the skill's default visual settings unless I specify otherwise."
policy:
  allow_implicit_invocation: true
